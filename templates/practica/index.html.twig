{% extends 'base.html.twig' %}

{% block title %}Practica index{% endblock %}

{% block body %}
    <h1>Practica index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for practica in practicas %}
            <tr>
                <td>{{ practica.id }}</td>
                <td>
                    <a href="{{ path('practica_show', {'id': practica.id}) }}">show</a>
                    <a href="{{ path('practica_edit', {'id': practica.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="2">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('practica_new') }}">Create new</a>
{% endblock %}
