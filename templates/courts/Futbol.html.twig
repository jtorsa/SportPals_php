<div class="court-helper"><div class="row pos_2">{% set break = false %}
                {% set vote = '' %}
                {% set voteBreak = false %}
                {% set user = '' %}<div class="col-md-12 col-sm-12 position " id="Portero-1">
                    {% for  participante, value in global.participantes %}
                        {% if participante == 'Portero1' %}
                            {% set user = value %}
                            {% set break = true %} 
                        {% endif %}
                    {% endfor %}
                    {% if break == true %}
                        <div class="prueba"><img src="/SportSpals/public/avatars/{{user}}" class="position-avatar" alt="" value="" >
                        {% for posicion, rate in global.votos %}
                            {% if posicion == user %}
                                {% set vote = rate %}
                                {% set voteBreak = true %} 
                            {% endif %}
                        {% endfor %}
                        {% if voteBreak == true %}
                        <div class="star-value">
                            {% if vote == 1 %}
                                1<span class="glyphicon glyphicon-star"></span>
                            {% endif %}
                            {% if vote == 2 %}
                                2<span class="glyphicon glyphicon-star"></span>
                            {% endif %}
                            {% if vote == 3 %}
                                3<span class="glyphicon glyphicon-star"></span>
                            {% endif %}
                            {% if vote == 4 %}
                                4<span class="glyphicon glyphicon-star"></span>
                            {% endif %}
                            {% if vote == 5 %}
                                5<span class="glyphicon glyphicon-star"></span>
                            {% endif %}
                        </div>   
                        {% endif %}
                        {% if app.user and app.user.avatar != user and app.user.avatar in global.participantes %}
                            <div class="vote">
                                <div class="row">
                                    Puntua al jugador
                                </div>
                                <form name="rate" id="rate_form" action="{{ path('valoracion_new_ajax') }}" >
                                    <div class="rating">
                                        <span class="star" value="{{user}}_5_{{global.evento.id}}">☆</span>
                                        <span class="star" value="{{user}}_4_{{global.evento.id}}">☆</span>
                                        <span class="star" value="{{user}}_3_{{global.evento.id}}">☆</span>
                                        <span class="star" value="{{user}}_2_{{global.evento.id}}">☆</span>
                                        <span class="star" value="{{user}}_1_{{global.evento.id}}">☆</span>
                                    </div>
                                </form>
                            </div>
                        {% endif %}
                    </div>
                    {% elseif global.rolled  or app.user is null %}
                        <div class="row">
                            Portero del equipo 1
                        </div>
                    {% else %}
                        <div class="row">
                            Portero del equipo 1
                        </div>
                        <div class="row">
                            <button class="btn-add"> 
                                <span class="glyphicon glyphicon-plus"></span>
                            </button>
                        </div>
                    {% endif %}
                </div></div><div class="row pos_2">{% set break = false %}
                {% set vote = '' %}
                {% set voteBreak = false %}
                {% set user = '' %}<div class="col-md-6 col-sm-6 position " id="DefensaDerecho-1">
                    {% for  participante, value in global.participantes %}
                        {% if participante == 'DefensaDerecho1' %}
                            {% set user = value %}
                            {% set break = true %} 
                        {% endif %}
                    {% endfor %}
                    {% if break == true %}
                        <div class="prueba"><img src="/SportSpals/public/avatars/{{user}}" class="position-avatar" alt="" value="" >
                        {% for posicion, rate in global.votos %}
                            {% if posicion == user %}
                                {% set vote = rate %}
                                {% set voteBreak = true %} 
                            {% endif %}
                        {% endfor %}
                        {% if voteBreak == true %}
                        <div class="star-value">
                            {% if vote == 1 %}
                                1<span class="glyphicon glyphicon-star"></span>
                            {% endif %}
                            {% if vote == 2 %}
                                2<span class="glyphicon glyphicon-star"></span>
                            {% endif %}
                            {% if vote == 3 %}
                                3<span class="glyphicon glyphicon-star"></span>
                            {% endif %}
                            {% if vote == 4 %}
                                4<span class="glyphicon glyphicon-star"></span>
                            {% endif %}
                            {% if vote == 5 %}
                                5<span class="glyphicon glyphicon-star"></span>
                            {% endif %}
                        </div>   
                        {% endif %}
                        {% if app.user and app.user.avatar != user and app.user.avatar in global.participantes %}
                            <div class="vote">
                                <div class="row">
                                    Puntua al jugador
                                </div>
                                <form name="rate" id="rate_form" action="{{ path('valoracion_new_ajax') }}" >
                                    <div class="rating">
                                        <span class="star" value="{{user}}_5_{{global.evento.id}}">☆</span>
                                        <span class="star" value="{{user}}_4_{{global.evento.id}}">☆</span>
                                        <span class="star" value="{{user}}_3_{{global.evento.id}}">☆</span>
                                        <span class="star" value="{{user}}_2_{{global.evento.id}}">☆</span>
                                        <span class="star" value="{{user}}_1_{{global.evento.id}}">☆</span>
                                    </div>
                                </form>
                            </div>
                        {% endif %}
                    </div>
                    {% elseif global.rolled  or app.user is null %}
                        <div class="row">
                            DefensaDerecho del equipo 1
                        </div>
                    {% else %}
                        <div class="row">
                            DefensaDerecho del equipo 1
                        </div>
                        <div class="row">
                            <button class="btn-add"> 
                                <span class="glyphicon glyphicon-plus"></span>
                            </button>
                        </div>
                    {% endif %}
                </div>{% set break = false %}
                {% set vote = '' %}
                {% set voteBreak = false %}
                {% set user = '' %}<div class="col-md-6 col-sm-6 position " id="DefensaIzquierdo-1">
                    {% for  participante, value in global.participantes %}
                        {% if participante == 'DefensaIzquierdo1' %}
                            {% set user = value %}
                            {% set break = true %} 
                        {% endif %}
                    {% endfor %}
                    {% if break == true %}
                        <div class="prueba"><img src="/SportSpals/public/avatars/{{user}}" class="position-avatar" alt="" value="" >
                        {% for posicion, rate in global.votos %}
                            {% if posicion == user %}
                                {% set vote = rate %}
                                {% set voteBreak = true %} 
                            {% endif %}
                        {% endfor %}
                        {% if voteBreak == true %}
                        <div class="star-value">
                            {% if vote == 1 %}
                                1<span class="glyphicon glyphicon-star"></span>
                            {% endif %}
                            {% if vote == 2 %}
                                2<span class="glyphicon glyphicon-star"></span>
                            {% endif %}
                            {% if vote == 3 %}
                                3<span class="glyphicon glyphicon-star"></span>
                            {% endif %}
                            {% if vote == 4 %}
                                4<span class="glyphicon glyphicon-star"></span>
                            {% endif %}
                            {% if vote == 5 %}
                                5<span class="glyphicon glyphicon-star"></span>
                            {% endif %}
                        </div>   
                        {% endif %}
                        {% if app.user and app.user.avatar != user and app.user.avatar in global.participantes %}
                            <div class="vote">
                                <div class="row">
                                    Puntua al jugador
                                </div>
                                <form name="rate" id="rate_form" action="{{ path('valoracion_new_ajax') }}" >
                                    <div class="rating">
                                        <span class="star" value="{{user}}_5_{{global.evento.id}}">☆</span>
                                        <span class="star" value="{{user}}_4_{{global.evento.id}}">☆</span>
                                        <span class="star" value="{{user}}_3_{{global.evento.id}}">☆</span>
                                        <span class="star" value="{{user}}_2_{{global.evento.id}}">☆</span>
                                        <span class="star" value="{{user}}_1_{{global.evento.id}}">☆</span>
                                    </div>
                                </form>
                            </div>
                        {% endif %}
                    </div>
                    {% elseif global.rolled  or app.user is null %}
                        <div class="row">
                            DefensaIzquierdo del equipo 1
                        </div>
                    {% else %}
                        <div class="row">
                            DefensaIzquierdo del equipo 1
                        </div>
                        <div class="row">
                            <button class="btn-add"> 
                                <span class="glyphicon glyphicon-plus"></span>
                            </button>
                        </div>
                    {% endif %}
                </div></div><div class="row pos_2">
                {% set break = false %}
                {% set vote = '' %}
                {% set voteBreak = false %}
                {% set user = '' %}<div class="col-md-6 col-sm-6 position " id="DefensaDerecho-2">
                    {% for  participante, value in global.participantes %}
                        {% if participante == 'DefensaDerecho2' %}
                            {% set user = value %}
                            {% set break = true %} 
                        {% endif %}
                    {% endfor %}
                    {% if break == true %}
                        <div class="prueba"><img src="/SportSpals/public/avatars/{{user}}" class="position-avatar" alt="" value="" >
                        {% for posicion, rate in global.votos %}
                            {% if posicion == user %}
                                {% set vote = rate %}
                                {% set voteBreak = true %} 
                            {% endif %}
                        {% endfor %}
                        {% if voteBreak == true %}
                            <div class="row class="position-avatar"">
                                {% if vote == 1 %}
                                    <span class="glyphicon glyphicon-star"></span>
                                {% endif %}
                                {% if vote == 2 %}
                                    <span class="glyphicon glyphicon-star"></span><span class="glyphicon glyphicon-star"></span>
                                {% endif %}
                                {% if vote == 3 %}
                                    <span class="glyphicon glyphicon-star"></span><span class="glyphicon glyphicon-star"></span><span class="glyphicon glyphicon-star"></span>
                                {% endif %}
                                {% if vote == 4 %}
                                    <span class="glyphicon glyphicon-star"></span><span class="glyphicon glyphicon-star"></span><span class="glyphicon glyphicon-star"></span><span class="glyphicon glyphicon-star"></span>
                                {% endif %}
                                {% if vote == 5 %}
                                    <span class="glyphicon glyphicon-star"></span><span class="glyphicon glyphicon-star"></span><span class="glyphicon glyphicon-star"></span><span class="glyphicon glyphicon-star"></span><span class="glyphicon glyphicon-star"></span>
                                {% endif %}
                            </div>   
                        {% endif %}
                        {% if app.user and app.user.avatar != user and app.user.avatar in global.participantes %}
                            <div class="vote">
                                <div class="row">
                                    Puntua al jugador
                                </div>
                                <form name="rate" id="rate_form" action="{{ path('valoracion_new_ajax') }}" >
                                    <div class="rating">
                                        <span class="star" value="{{user}}_5_{{global.evento.id}}">☆</span>
                                        <span class="star" value="{{user}}_4_{{global.evento.id}}">☆</span>
                                        <span class="star" value="{{user}}_3_{{global.evento.id}}">☆</span>
                                        <span class="star" value="{{user}}_2_{{global.evento.id}}">☆</span>
                                        <span class="star" value="{{user}}_1_{{global.evento.id}}">☆</span>
                                    </div>
                                </form>
                            </div>
                        {% endif %}
                    </div>
                    {% elseif global.rolled  or app.user is null %}
                        <div class="row">
                            DefensaDerecho del equipo 2
                        </div>
                    {% else %}
                        <div class="row">
                            DefensaDerecho del equipo 2
                        </div>
                        <div class="row">
                            <button class="btn-add"> 
                                <span class="glyphicon glyphicon-plus"></span>
                            </button>
                        </div>
                    {% endif %}
                </div><div class="col-md-6 col-sm-6 position " id="DefensaIzquierdo-2">
                    {% for  participante, value in global.participantes %}
                        {% if participante == 'DefensaIzquierdo2' %}
                            {% set user = value %}
                            {% set break = true %} 
                        {% endif %}
                    {% endfor %}
                    {% if break == true %}
                        <div class="prueba"><img src="/SportSpals/public/avatars/{{user}}" class="position-avatar" alt="" value="" >
                        {% for posicion, rate in global.votos %}
                            {% if posicion == user %}
                                {% set vote = rate %}
                                {% set voteBreak = true %} 
                            {% endif %}
                        {% endfor %}
                        {% if voteBreak == true %}
                            <div class="row class="position-avatar"">
                                {% if vote == 1 %}
                                    <span class="glyphicon glyphicon-star"></span>
                                {% endif %}
                                {% if vote == 2 %}
                                    <span class="glyphicon glyphicon-star"></span><span class="glyphicon glyphicon-star"></span>
                                {% endif %}
                                {% if vote == 3 %}
                                    <span class="glyphicon glyphicon-star"></span><span class="glyphicon glyphicon-star"></span><span class="glyphicon glyphicon-star"></span>
                                {% endif %}
                                {% if vote == 4 %}
                                    <span class="glyphicon glyphicon-star"></span><span class="glyphicon glyphicon-star"></span><span class="glyphicon glyphicon-star"></span><span class="glyphicon glyphicon-star"></span>
                                {% endif %}
                                {% if vote == 5 %}
                                    <span class="glyphicon glyphicon-star"></span><span class="glyphicon glyphicon-star"></span><span class="glyphicon glyphicon-star"></span><span class="glyphicon glyphicon-star"></span><span class="glyphicon glyphicon-star"></span>
                                {% endif %}
                            </div>   
                        {% endif %}
                        {% if app.user and app.user.avatar != user and app.user.avatar in global.participantes %}
                            <div class="vote">
                                <div class="row">
                                    Puntua al jugador
                                </div>
                                <form name="rate" id="rate_form" action="{{ path('valoracion_new_ajax') }}" >
                                    <div class="rating">
                                        <span class="star" value="{{user}}_5_{{global.evento.id}}">☆</span>
                                        <span class="star" value="{{user}}_4_{{global.evento.id}}">☆</span>
                                        <span class="star" value="{{user}}_3_{{global.evento.id}}">☆</span>
                                        <span class="star" value="{{user}}_2_{{global.evento.id}}">☆</span>
                                        <span class="star" value="{{user}}_1_{{global.evento.id}}">☆</span>
                                    </div>
                                </form>
                            </div>
                        {% endif %}
                    </div>
                    {% elseif global.rolled  or app.user is null %}
                        <div class="row">
                            DefensaIzquierdo del equipo 2
                        </div>
                    {% else %}
                        <div class="row">
                            DefensaIzquierdo del equipo 2
                        </div>
                        <div class="row">
                            <button class="btn-add"> 
                                <span class="glyphicon glyphicon-plus"></span>
                            </button>
                        </div>
                    {% endif %}
                </div></div><div class="row pos_2">
                {% set break = false %}
                {% set vote = '' %}
                {% set voteBreak = false %}
                {% set user = '' %}<div class="col-md-12 col-sm-12 position " id="Portero-2">
                    {% for  participante, value in global.participantes %}
                        {% if participante == 'Portero2' %}
                            {% set user = value %}
                            {% set break = true %} 
                        {% endif %}
                    {% endfor %}
                    {% if break == true %}
                        <div class="prueba"><img src="/SportSpals/public/avatars/{{user}}" class="position-avatar" alt="" value="" >
                        {% for posicion, rate in global.votos %}
                            {% if posicion == user %}
                                {% set vote = rate %}
                                {% set voteBreak = true %} 
                            {% endif %}
                        {% endfor %}
                        {% if voteBreak == true %}
                            <div class="row class="position-avatar"">
                                {% if vote == 1 %}
                                    <span class="glyphicon glyphicon-star"></span>
                                {% endif %}
                                {% if vote == 2 %}
                                    <span class="glyphicon glyphicon-star"></span><span class="glyphicon glyphicon-star"></span>
                                {% endif %}
                                {% if vote == 3 %}
                                    <span class="glyphicon glyphicon-star"></span><span class="glyphicon glyphicon-star"></span><span class="glyphicon glyphicon-star"></span>
                                {% endif %}
                                {% if vote == 4 %}
                                    <span class="glyphicon glyphicon-star"></span><span class="glyphicon glyphicon-star"></span><span class="glyphicon glyphicon-star"></span><span class="glyphicon glyphicon-star"></span>
                                {% endif %}
                                {% if vote == 5 %}
                                    <span class="glyphicon glyphicon-star"></span><span class="glyphicon glyphicon-star"></span><span class="glyphicon glyphicon-star"></span><span class="glyphicon glyphicon-star"></span><span class="glyphicon glyphicon-star"></span>
                                {% endif %}
                            </div>   
                        {% endif %}
                        {% if app.user and app.user.avatar != user and app.user.avatar in global.participantes %}
                            <div class="vote">
                                <div class="row">
                                    Puntua al jugador
                                </div>
                                <form name="rate" id="rate_form" action="{{ path('valoracion_new_ajax') }}" >
                                    <div class="rating">
                                        <span class="star" value="{{user}}_5_{{global.evento.id}}">☆</span>
                                        <span class="star" value="{{user}}_4_{{global.evento.id}}">☆</span>
                                        <span class="star" value="{{user}}_3_{{global.evento.id}}">☆</span>
                                        <span class="star" value="{{user}}_2_{{global.evento.id}}">☆</span>
                                        <span class="star" value="{{user}}_1_{{global.evento.id}}">☆</span>
                                    </div>
                                </form>
                            </div>
                        {% endif %}
                    </div>
                    {% elseif global.rolled  or app.user is null %}
                        <div class="row">
                            Portero del equipo 2
                        </div>
                    {% else %}
                        <div class="row">
                            Portero del equipo 2
                        </div>
                        <div class="row">
                            <button class="btn-add"> 
                                <span class="glyphicon glyphicon-plus"></span>
                            </button>
                        </div>
                    {% endif %}
                </div></div></div>