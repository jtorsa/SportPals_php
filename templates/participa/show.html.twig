{% extends 'base.html.twig' %}

{% block title %}Participa{% endblock %}

{% block body %}
    <h1>Participa</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ participa.id }}</td>
            </tr>
            <tr>
                <th>Posicion</th>
                <td>{{ participa.posicion }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('participa_index') }}">back to list</a>

    <a href="{{ path('participa_edit', {'id': participa.id}) }}">edit</a>

    {{ include('participa/_delete_form.html.twig') }}
{% endblock %}
